<Window x:Class="RandomScoreAllocatorWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="随机分数分配器" Height="768" Width="1024" 
        Background="#F0F2F5" 
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" TextOptions.TextFormattingMode="Display"
        WindowStyle="None" 
        ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryDark" Color="#222831"/>
        <SolidColorBrush x:Key="PrimaryTeal" Color="#00ADB5"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#6C5CE7"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#00B894"/>
        <SolidColorBrush x:Key="TextGray" Color="#636E72"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#DFE6E9"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryTeal}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                <Setter TargetName="border" Property="Margin" Value="0,1,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource PrimaryDark}" Height="60" Padding="20,0" 
                MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="24" Height="24" Margin="0,0,12,0">
                        <Image Source="/brsjc-uyor5-001.ico" 
           RenderOptions.BitmapScalingMode="HighQuality" 
           HorizontalAlignment="Center" 
           VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="随机分数分配器" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Button Content="说明" Background="Transparent" BorderThickness="0" Foreground="#AAA" Click="BtnHelp_Click" Margin="0,0,10,0"/>
                    <Button Content="—" Width="40" Background="Transparent" BorderThickness="0" Click="BtnMinimize_Click" FontSize="14"/>
                    <Button Content="✕" Width="50" Background="#FF4757" BorderThickness="0" Click="BtnClose_Click" Margin="10,0,10,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="20,20,20,10" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="EXCEL 源文件" Style="{StaticResource LabelStyle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtFile" IsReadOnly="True" Text="请选择文件..." Foreground="#999" Margin="0,0,8,0"/>
                        <Button Grid.Column="1" Content="浏览..." Background="{StaticResource AccentPurple}" Click="BtnBrowse_Click"/>
                    </Grid>
                    <TextBlock Text="格式: 第一行需包含 姓名 | 学科(分数)|总分（分数）" Foreground="#B2BEC3" FontSize="11" Margin="2,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="生成参数" Style="{StaticResource LabelStyle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="单科最低分" FontSize="11" Foreground="#636E72"/>
                            <TextBox x:Name="TxtMinEach" Text="5" Margin="0,4,0,0" HorizontalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">

                            <CheckBox x:Name="ChkFixedSeed" Content="固定随机种子" IsChecked="True" VerticalAlignment="Bottom" FontSize="12" Foreground="#2D3436"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="4" VerticalAlignment="Top" Margin="0,18,0,0">
                    <Button Content="生成预览数据" Background="{StaticResource AccentGreen}" Click="BtnPreview_Click" Padding="24,10" FontSize="14"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="20,0,20,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="16,12">
                    <DockPanel>
                        <TextBlock Text="数据预览" FontWeight="Bold" FontSize="14" Foreground="#2D3436" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="预览仅显示前 50 条" Foreground="#B2BEC3" FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <DataGrid Grid.Row="1" x:Name="GridPreview" BorderThickness="0" Background="White" 
                          AutoGenerateColumns="True" CanUserAddRows="False" IsReadOnly="True"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F0F0" RowHeaderWidth="0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Background="White" Height="60">
            <Border BorderBrush="#EEEEEE" BorderThickness="0,1,0,0">
                <DockPanel Margin="20,0" LastChildFill="False">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="准备就绪" Foreground="#00B894" FontWeight="SemiBold" FontSize="12"/>
                    </StackPanel>
                    <Button DockPanel.Dock="Right" Content="导出结果到 Excel" Background="{StaticResource PrimaryTeal}" Click="BtnExport_Click" VerticalAlignment="Center" Padding="20,8"/>
                </DockPanel>
            </Border>
        </Grid>

    </Grid>
</Window>